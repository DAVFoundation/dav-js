export enum EnergySources {
  Grid = 'grid',
  Renewable = 'renewable',
  Solar = 'solar',
  Wind = 'wind',
  Hydro = 'hydro',
  Geothermal = 'geothermal',
}

export enum Amenities {
  Park = 1,
}

export enum PriceTypes {
  kwh = 'kwh',
  second = 'second',
  minute = 'minute',
  hour = 'hour',
  day = 'day',
  week = 'week',
  flat = 'flat',
}
