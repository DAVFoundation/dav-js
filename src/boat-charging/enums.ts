export enum EnergySources {
    grid = 'grid',
    renewable = 'renewable',
    solar = 'solar',
    wind = 'wind',
    hydro = 'hydro',
    geothermal = 'geothermal',
}

export enum Amenities {
    Lodging = 1,
    Dining = 1,
    Restrooms = 1,
    Docking = 1,
    Park = 1,
    WiFi = 1,
    Shopping = 1,
    Grocery = 1,
}

export enum PriceTypes {
    kwh = 'kwh',
    second = 'second',
    minute = 'minute',
    hour = 'hour',
    day = 'day',
    week = 'week',
    flat = 'flat',
}
